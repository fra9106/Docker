<?php

namespace App\DataFixtures\Helper;

trait RandomBooleanTrait
{
    private function mtRandBool(): bool
    {
        $boolean = mt_rand(0, 1);

        if ($boolean) {
            return true;
        } else {
            return false;
        }
    }
}